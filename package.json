{
  "name": "contextos-ai",
  "version": "0.0.0",
  "private": true,
  "description": "Production-grade AI SaaS monorepo",
  "license": "UNLICENSED",
  "author": "Yash <itu.yash3003@gmail.com>",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "clean": "turbo run clean",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:push": "turbo run db:push",
    "db:studio": "turbo run db:studio",
    "format": "prettier --write . --cache --ignore-unknown",
    "format:check": "prettier --check . --cache --ignore-unknown",
    "check": "pnpm format:check && pnpm lint && pnpm type-check",
    "prepare": "echo \"(placeholder) husky install will be wired up later\""
  },
  "devDependencies": {
    "@contextos-ai/prettier-config": "workspace:*",
    "@prisma/client": "5.22.0",
    "@types/node": "^25.8.0",
    "dotenv": "^17.4.2",
    "eslint": "^10.3.0",
    "prettier": "^3.8.3",
    "prisma": "5.22.0",
    "tsx": "^4.22.0",
    "turbo": "^2.9.12",
    "typescript": "^6.0.3"
  }
}
