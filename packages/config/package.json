{
  "name": "@contextos-ai/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed runtime configuration and environment variable validation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo build placeholder",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "type-check": "tsc --noEmit",
    "clean": "echo clean placeholder"
  },
  "devDependencies": {
    "@contextos-ai/eslint-config": "workspace:*",
    "@contextos-ai/tsconfig": "workspace:*"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
